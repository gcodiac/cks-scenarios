# Check

Placeholder check: show evidence that **Implement container runtime security** is configured correctly and that the cluster remains healthy.

```bash
kubectl get nodes
kubectl get events -A --sort-by=.lastTimestamp
```

Replace this text with the expected verification and follow-up questions.
