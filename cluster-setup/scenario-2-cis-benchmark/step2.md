# Stage 2 – Required remediation

Open `cis-benchmark-required/step1.md` and `cis-benchmark-required/step2.md`. Harden the `cis-required` namespace with restricted Pod Security Admission, a dedicated service account without automatic token mounting, non-root execution, RuntimeDefault seccomp, and default-deny network policy.
