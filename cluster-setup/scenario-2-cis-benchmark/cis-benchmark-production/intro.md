# CIS Kubernetes Benchmark – Production Program

Production compliance is a controlled process, not a one-time `kube-bench` run. In this lab you will define scope, pin tool versions, retain evidence, assign remediation owners, and document justified exceptions.

Never run an unreviewed remediation script against production. Back up configuration, use change control, and test changes against the Kubernetes version and distribution you actually operate.
