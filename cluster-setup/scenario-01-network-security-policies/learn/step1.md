# Task

Placeholder question: what is the secure configuration required for **Use Network Security Policies to restrict cluster-level access**?

Inspect the environment and perform the task:

```bash
kubectl get --raw='/readyz'
kubectl get nodes
```

Replace this text with the stage-specific exercise.
