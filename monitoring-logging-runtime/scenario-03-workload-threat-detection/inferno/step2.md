# Check

Placeholder check: show evidence that **Detect threats within workloads** is configured correctly and that the cluster remains healthy.

```bash
kubectl get nodes
kubectl get events -A --sort-by=.lastTimestamp
```

Replace this text with the expected verification and follow-up questions.
