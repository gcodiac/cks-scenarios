# Required review

Check that your answer is narrow, reproducible, and reversible. Record the before state, the command or manifest used, and the after-state evidence. If a check fails, inspect events and the rendered object instead of immediately broadening permissions.
